[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn carmarket.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.healthcheck]
path = "/api/cars/categories/"
timeout = 30
